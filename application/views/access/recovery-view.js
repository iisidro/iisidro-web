// VENDOR LIBS
const React = require('react');

// COMPONENTS APP
const RecoveryForm = require('components-app/access/recovery-form');

const RecoveryView = React.createClass({

    render () {
        return (
            <RecoveryForm />
        );
    }
});

module.exports = RecoveryView;